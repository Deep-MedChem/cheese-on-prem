#!/bin/bash
# Stop the cheese-search-ui (leaves the backend running).

set -e
source "$(dirname "$(readlink -f "$0")")/_compose-env"

echo "Stopping CHEESE UI..."
$COMPOSE_CMD stop ui
echo "Stopped."

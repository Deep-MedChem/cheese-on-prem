{{- /*
Inline (self-contained) licence-key Secret. Skipped when
licenseAgent.secret.existingSecret is set — then the client's own Secret
(Vault / External Secrets Operator / SealedSecrets / `kubectl create secret`)
is referenced by name and MUST carry the same key: `licenseKey`.
*/ -}}
{{- if and .Values.licenseAgent.enabled (not .Values.licenseAgent.secret.existingSecret) -}}
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: cheese-license-key
  labels:
    {{- include "cheese.componentLabels" (list . "license-agent") | nindent 4 }}
stringData:
  licenseKey: {{ .Values.licenseAgent.secret.licenseKey | quote }}
{{- end }}
